version: "2.3"
services:

  proxy-connect:
    hostname: proxy-connect
    build:
      context: ../cluster
      dockerfile: proxy-connect/Dockerfile
      args:
        - CONNECT_BINARY_URL=rstudio-connect_1.6.6.1-4_amd64.deb
    privileged: true
    environment:
      - CONNECT_LICENSE

    
